# 後端溝通紀錄 — 2025-12-16

1. `/vendors/:id` PATCH 失敗：會回傳 `Invalid vendor data`。

2. `/vendors` POST PATCH 表單欄位需求（請協助確認欄位或簡化方式）：
   - ~~公司名稱~~（對應 `name`）
   - ~~商品類別（`productTypeCode` | `productTypeId`）~~
   - 公司電話（缺欄位；需與聯絡電話分開）
   - ~~電子信箱~~（`email`）
   - 公司地址（現有欄位拆成 street/city/state/zipCode/country，請將地址改成單一欄位）
   - ~~聯絡人~~（`contactPerson`）
   - ~~聯絡電話（`phone`）~~
   - 公司帳戶戶名（缺欄位）
   - 公司銀行帳號（缺欄位）
   - 銀行名稱（缺欄位）
   - 分行名稱（缺欄位）
   - 備註（缺欄位）
   - ~~狀態（啟用/停用）（`isActive`）~~

3. `/vendors` GET 查詢條件（請協助確認欄位或補齊）：
   - ~~供應商名稱 (`search` 模糊搜尋 name/code)~~
   - ~~聯絡人姓名 (`search` 模糊搜尋 contactPerson)~~
   - ~~聯絡人電話 (`search` 模糊搜尋 phone)~~
   - ~~電子信箱 (`search` 模糊搜尋 email)~~
   - ~~產品類型 (`productTypeCode` / `productTypeId`)~~
   - 地址（缺欄位；請提供可依 address/城市 進行篩選的參數）
   - ~~狀態 (`status` 或 `isActive`)~~
   
4. `/vendors` GET 排序條件（請協助確認欄位或補齊）：
   - ~~供應商名稱 (`sortBy=name`)~~
   - ~~聯絡人姓名 (`sortBy=contactPerson`)~~
   - ~~建立時間 (`sortBy=createdAt`)~~
   - ~~更新時間 (`sortBy=updatedAt`)~~
   - 地址（缺欄位；請提供 `sortBy=address` ）
   - 產品類型（缺欄位；請提供 `sortBy=productTypeCode` / `sortBy=productTypeId` ）

5. `/vendors/:id` DELETE 實測結果僅將 `isActive` 改為 false（列表仍顯示該筆資料、狀態變成停用）。請確認這隻 API 是否預期為 soft delete，若是，麻煩在文件/回應訊息中明確說明；若需要支援真正刪除，也請提供對應方式以便前端調整 UI 與提示文案。

6. `/users` POST PATCH 表單欄位需求（請協助確認欄位或簡化方式）：
   - 姓名（請改為單一欄位或接受完整姓名；目前需同時送 `firstName`、`lastName`）
   - 身分證號碼（缺欄位）
   - 生日（缺欄位）
   - 職務類型（缺欄位；需求包含「董事長/總經理/業務經理/業務專員/會計/客服/倉儲/送貨司機/其他」，需提供欄位或對照表）
   - 職務類型的「其他」欄位（缺欄位，用於補充自訂職稱）
   - ~~手機號碼（`phone`）~~
   - ~~電子信箱（`email`）~~
   - 地址（缺欄位）
   - 入職日期（缺欄位）
   - ~~狀態（啟用/停用）（`isActive` / `status`）~~
   - 登入密碼（放寬複雜度）
   - ~~權限（admin/user）~~（`roleNames`）

7. `/users` GET 查詢條件（請協助確認欄位或補齊）：
   - ~~姓名 (`name`)~~
   - 身分證字號（缺欄位）
   - ~~職務 (`position`)~~
   - 手機號碼（缺欄位）
   - ~~電子信箱 (`email`)~~
   - 入職日期（缺欄位）
   - ~~狀態 (`status`)~~

8. `/users` GET 排序條件（請協助確認欄位或補齊）：
   - ~~建立時間 (`sortField=createdAt`)~~
   - ~~更新時間 (`sortField=updatedAt`)~~
   - ~~電子信箱 (`sortField=email`)~~
   - ~~名 (`sortField=firstName`)~~
   - ~~姓 (`sortField=lastName`)~~
   - ~~最後登入時間 (`sortField=lastLoginAt`)~~

9. `/customers` POST PATCH 表單欄位需求（請協助確認欄位或簡化方式）：
   - ~~聯絡人列表 (`customFields.contacts`)~~
     - ~~聯絡人名稱 (`customFields.contacts[0].name`)~~
     - ~~聯絡電話 (`customFields.contacts[0].phone`)~~
     - ~~聯絡地址 (`customFields.contacts[0].address`)~~
     - ~~電子信箱 (`customFields.contacts[0].email`)~~
     - ~~是否為主要聯絡人 (`isPrimary`)~~
   - 公司資訊
     - ~~名稱 (`name`)~~
     - ~~電話 (`contactInfo.phone`)~~
     - ~~信箱 (`contactInfo.email`)~~
     - 地址（現有欄位拆成 street/city/state/zipCode/country，請將地址改成單一欄位）
     - ~~統一編號 (`taxId`)~~
   - 其他資訊：
     - ~~客戶類型（公司戶、個人戶）(`type`)~~
     - ~~客戶分類（零售、批發、VIP、企業戶）(`segment`)~~
     - ~~客戶來源（官網、客戶轉介、電話開發、社群、展覽、其他）(`source`)~~
     - ~~業務負責 ID (`salesRepId`)~~
     - ~~標籤 (`tags`)~~
     - ~~客戶類別（桶裝水/雞蛋/飲水機，多選）(`categories`)~~
     - ~~出貨星期（星期日到星期六，多選）(`deliveryDays`)~~
     - ~~收付方式（現金/月結/預付）(`paymentMethod`)~~
     - ~~訂金金額 (`deposit`)~~
     - ~~發票抬頭 (`invoiceTitle`)~~
     - ~~發票統編 (`invoiceTaxId`)~~
     - ~~備註 (`notes`)~~
     - ~~狀態（啟用、停用）→ `status`~~
     - ~~商品價格調整 (`customPrices`)~~
       - ~~商品ID (`customPrices[0].productId`)~~
       - ~~調整金額 (`customPrices[0].adjustment`)~~

10. `/customers` GET 查詢條件（請協助確認欄位或簡化方式）：
    - 關鍵字 (`search`) (目前只能模糊搜尋name/phone/email；請改成可支援模糊搜尋「主要聯絡人姓名/主要聯絡人電話/主要聯絡人地址/主要聯絡人電子信箱/公司名稱/公司電話/統一編號/公司地址/公司電子信箱/備註欄位」)
    - ~~客戶類型 (`type`)~~
    - ~~客戶分類(RETAIL(零售)、WHOLESALE(批發)、VIP(VIP)、CORPORATE(企業戶)) (`segment`)~~
    - ~~客戶來源(WEBSITE(官網), REFERRAL(客戶轉介), COLD_CALL(電話開發), SOCIAL_MEDIA(網路廣告), TRADE_SHOW(展覽), OTHER(其他)) (`segment`)~~
    - ~~業務負責ID(填入users id) (`salesRepId`)~~
    - ~~標籤(關鍵字) (`tags`)~~
    - ~~狀態(啟用、停用) (`status`)~~

11. `/customers` GET 排序條件（請協助確認欄位或補齊）：
    - ~~客戶名稱 (`sortBy=name`)~~
    - ~~客戶類型 (`sortBy=type`)~~
    - ~~客戶分類 (`sortBy=segment`)~~
    - ~~客戶來源 (`sortBy=source`)~~
    - ~~業務負責 ID (`sortBy=salesRepId`)~~
    - ~~建立時間 (`sortBy=createdAt`)~~
    - ~~狀態 (`sortBy=status`)~~
    - 標籤 (缺欄位；請提供 `sortBy=tags`)
    - 統一編號 (缺欄位；請提供 `sortBy=taxId`)

12. `/products` POST PATCH 表單欄位需求（請協助確認欄位或補齊）：
    - ~~名稱 (`name`)~~
    - ~~單位 (`unit`)~~
    - ~~建議售價 (`basePrice.amount`)~~
    - ~~批發價格 (`wholesalePrice.amount`)~~
    - ~~現金價格 (`cashPrice.amount`)~~
    - ~~成本金額 (`costPrice.amount`)~~
    - ~~最低安全庫存量 (`minStock`)~~
    - ~~最高安全庫存量 (`maxStock`)~~
    - ~~最低需補貨存量 (`reorderPoint`)~~
    - ~~供應商 ID (`primaryVendorId`)~~
    - ~~標籤 (`tags`)~~
    - ~~狀態 (`status`)~~
    - ~~備註欄位（`description`）~~
    - ~~是否需冷藏 (`isPerishable`)~~

13. `/products` GET 查詢條件（請協助確認欄位或補齊）：
    - 名稱（目前只能用 `search` 模糊查詢，請增加獨立 `name` 參數）
    - 商品編號（同上，需提供獨立 `code` 篩選）
    - 單位（缺欄位；需可依 `unit` 篩選）
    - ~~建議售價最低～最高 (`minPrice` / `maxPrice`)~~
    - 批發價格最低～最高（缺欄位；請提供 `minWholesalePrice` / `maxWholesalePrice` 或類似參數）
    - 現金價格最低～最高（缺欄位；請提供 `minCashPrice` / `maxCashPrice`）
    - 成本金額最低～最高（缺欄位；請提供 `minCostPrice` / `maxCostPrice`）
    - ~~是否低於需補貨存量 (`lowStock`)~~
    - ~~是否有庫存 (`inStock`)~~
    - ~~供應商 ID (`primaryVendorId`)~~
    - ~~是否需冷藏 (`isPerishable`)~~
    - ~~標籤 (`tags`)~~
    - ~~狀態 (`status`)~~

14. `/products` GET 排序條件（請協助確認欄位或補齊）：
    - ~~商品名稱 (`sortBy=name`)~~
    - ~~商品編號 (`sortBy=code`)~~
    - 建議售價 (`sortBy=basePrice`) → 目前會 500，實際欄位為 `basePriceAmount`，請協助修正文件或提供可用欄位
    - 單位（缺欄位；請提供 `sortBy=unit` ）
    - 供應商（缺欄位；請提供 `sortBy=primaryVendorId` ）
    - 是否需冷藏（缺欄位；請提供 `sortBy=isPerishable` ）
    - 狀態（缺欄位；請提供 `sortBy=status` ）
    - ~~當前庫存 (`sortBy=currentStock`)~~
    - ~~更新時間 (`sortBy=updatedAt`)~~
