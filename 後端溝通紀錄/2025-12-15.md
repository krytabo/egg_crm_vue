# 後端溝通紀錄 — 2025-12-15

1. ~~`/products` API 使用 `sortBy=basePrice` 時會回傳 500（Internal Server Error），推測是後端仍以 `basePrice` 欄位排序，實際資料表欄位為 `basePriceAmount`。目前前端改為暫時不提供該排序，請協助調
   整 API 以支援。~~（2025-12-16 第 14 點已補充詳細需求）
2. ~~產品列表尚無法依「單位 (unit)」、「供應商 (primaryVendorId)」、「冷藏 (isPerishable)」、「狀態 (status)」進行排序，但 UI 已有相對應欄位需求。若未來後端能提供這些排序欄位，前端即可開放使用。~~ 2025-12-16 第 9 點統一說明）
3. 潛在客戶 API (`/potential-customers`) 目前僅在文件中，缺少正式的欄位/回傳樣板，先依文件假設欄位實作。若後端實際欄位不同（例如 contacts 結構、source 列舉值、tags 傳遞形式），請提供正式結構以便前端調整。
4. ~~`/vendors/:id` PATCH 目前即使送出符合文件的 payload（例如僅更新 `name/contactPerson/email/phone/address/productTypeCode/paymentTerms/isActive`，所有欄位皆為非空字串），仍一律回傳 `Invalid vendor data`。經前端多次確認請求內容與 Swagger 範例一致，推測是後端在進行 `UpdateVendorValidationPipe` 時 body 被視為空物件，請協助檢查實際收到的 request body 並提供可接受的欄位組合、必要時調整 API。~~（2025-12-16 重新補充於下方）
